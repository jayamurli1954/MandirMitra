PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000
PLAYWRIGHT_BACKEND_URL=http://127.0.0.1:8000
PLAYWRIGHT_TEST_USERNAME=admin@temple.com
PLAYWRIGHT_TEST_PASSWORD=replace-with-real-password
# Set PLAYWRIGHT_SKIP_WEBSERVER=1 when the frontend is already running.
# Set PLAYWRIGHT_SKIP_WEBSERVER=1 and PLAYWRIGHT_BASE_URL to a deployed URL to test cloud environments.

PLAYWRIGHT_APP_NAME=MandirMitra
PLAYWRIGHT_APP_VARIANT=mandirmitra
